export const generateSitemap = async (host) => {
	// const resp = await axios.get(
	//   host + PAGES_API_ENDPOINT + '/?type=pages.BasePage&fields=last_published_at'
	// )
	// return resp.data.items.map((page) => {
	//   return {
	//     url: page.meta.html_url.replace(host, ''),
	//     changefreq: 'daily',
	//     priority: 1,
	//     lastmod: page.last_published_at,
	//   }
	// })
};
